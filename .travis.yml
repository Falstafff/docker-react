sudo: required

services:
  - docker

before_install:
  - docker build -t falstafff/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true falstafff/docker-react npm run test

language: generic